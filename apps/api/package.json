{
  "name": "@albumsl/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@albumsl/application": "0.0.0",
    "@albumsl/contracts": "0.0.0",
    "@albumsl/infra-firebase": "0.0.0",
    "firebase-admin": "^13.3.0"
  }
}
